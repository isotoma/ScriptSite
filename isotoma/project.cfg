[buildout]
eggs = south

[django]
project = scriptsite 
apps = main
settings = settings

test = ${django:apps}
projectegg = ${django:project}
eggs = ${buildout:eggs}
external_apps = south

[versions]
south = 0.7.2
